<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    meta(http-equiv="X-UA-Compatible", content="ie=edge")
    title Request Header Parser Microservice
    link(rel="stylesheet", href="styles.css", type="text/css")
  body
    h1.title Request Header Parser Microservice
    .container
      h2.subtitle.subtitle--no-top-margin Description:
      p.container__text Simple API that parses a request header and returns a JSON object containing the client's IP address, preferred languages and system info.
      h2.subtitle API Endpoint:
      a.container__text(href=endpoint) /api/whoami
      h2.subtitle Sample Output:
      code.sample-output
        ='{"ipaddress":"159.20.14.100","language":"en-US,en;q=0.5", "software":"Mozilla/5.0 (X11; Ubuntu; Linux x86_64; rv:50.0) Gecko/20100101 Firefox/50.0"}'
